server:
  port: 8888

spring:
  application:
    name: system-register-server

eureka:
  instance:
    hostname: 127.0.0.1
  server:
    enable-self-preservation: false
    url: http://127.0.0.1:8888

  client:
    registerWithEureka: false
    fetchRegistry: false
